{
  "name": "portfolio-fullstack",
  "version": "1.0.0",
  "description": "Full-stack portfolio application",
  "scripts": {
    "build": "echo 'Building frontend...' && cd frontend && npm install && npm run build && echo 'Setting up backend...' && cd ../backend && npm install && npx prisma generate && npx prisma migrate deploy && echo 'Build complete!'",
    "start": "cd backend && node index.js",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev"
  },
  "keywords": ["portfolio", "react", "express", "prisma"],
  "author": "",
  "license": "ISC"
}